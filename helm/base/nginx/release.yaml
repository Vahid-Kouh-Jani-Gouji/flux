apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: HelmRelease
metadata:
  name: nginx
  namespace: nginx
spec:
  chart:
    spec:
      chart: nginx-ingress
      version: 1.2.*
      interval: 5h
      sourceRef:
        kind: HelmRepository
        name: nginx
  interval: 5h
  releaseName: nginx
  targetNamespace: nginx
  
